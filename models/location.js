
class Location {
  constructor(title, description, schedule, number, urlImage, urlLocation) {
    this.title = title;
    this.description = description;
    this.schedule = schedule;
    this.number = number;
    this.urlImage = urlImage;
    this.urlLocation = urlLocation;
  }
}

module.exports = Location